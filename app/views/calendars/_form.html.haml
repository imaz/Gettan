= form_for @calendar do |f|
  -if @calendar.errors.any?
    #error_explanation
      %h2= "#{pluralize(@calendar.errors.count, "error")} prohibited this calendar from being saved:"
      %ul
        - @calendar.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :school_day
    = f.date_select :school_day
    %br/
    = f.label :memo
    = f.text_field :memo
  .actions
    = f.submit 'Save'
